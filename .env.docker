# ================================
# Docker Compose 环境变量配置
# ================================
# 用于 docker-compose.yml
# ================================

# ===== 应用配置 =====

# 应用名称
COMPOSE_PROJECT_NAME=random-food

# ===== 前端配置 =====

# Next.js 端口（宿主机访问端口）
FRONTEND_PORT=9090

# API 基础路径（容器内通信）
NEXT_PUBLIC_API_URL=/api/backend

# ===== 后端配置 =====

# 后端服务监听地址
# 注意：容器内必须使用 0.0.0.0 以允许跨容器通信和端口映射
# docker-compose.yml 已配置宿主机绑定为 127.0.0.1（安全）
BACKEND_HOST=0.0.0.0

# 后端服务端口（容器内端口）
BACKEND_PORT=9091

# ===== 端口映射安全说明 =====
# 宿主机端口绑定策略：
# - 默认绑定 127.0.0.1（只允许本机访问，安全）
# - 如需局域网访问（如手机测试），修改 docker-compose.yml：
#   将 "127.0.0.1:9090:9090" 改为 "0.0.0.0:9090:9090"
# - 生产环境强烈建议使用 127.0.0.1 + Nginx 反向代理

# Node 环境
NODE_ENV=production

# ===== 数据库配置 =====

# SQLite 数据库路径（容器内）
DATABASE_PATH=/app/food_db.sqlite

# ===== 日志配置 =====

# 日志级别 (debug, info, warn, error)
LOG_LEVEL=info

# ===== 其他配置 =====

# 时区设置
TZ=Asia/Shanghai
