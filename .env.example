# 环境变量示例文件
# ===== API 配置 =====

# API 基础路径
# 开发环境：使用相对路径，通过 Next.js rewrites 代理
# 生产环境：使用相对路径或完整 URL
NEXT_PUBLIC_API_URL=/api/backend

# 后端服务配置（用于 Next.js rewrites）
# BACKEND_HOST: 后端监听地址
#   - 127.0.0.1: 只允许本地访问（开发/生产环境推荐）
#   - 0.0.0.0: 允许所有网络访问（Docker 环境必需）
BACKEND_HOST=127.0.0.1

# 后端服务端口
BACKEND_PORT=9091

# ===== 其他配置 =====
# 根据需要添加其他环境变量，例如：
# DATABASE_URL=
# SECRET_KEY=
